{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/grpc-client/grpc-client.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}